---
phase: "02"
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - files/quantum-math-lab/analyze_q14_ole_benchmark.py
  - files/quantum-math-lab/results/ole/q14_ole_vs_delta2_benchmark.md
  - files/quantum-math-lab/results/ole/q14_ole_vs_delta2_benchmark.csv
interactive: false

conventions:
  units: "dimensionless circuit observables; runtime metrics in seconds"
  metric: "euclidean bookkeeping default"
  coordinates: "Qiskit qubit indices 0..n-1"
  operator_picture: "Benchmark panels report F_delta(Z_0) in the fixed A_P(U) picture"
  observable_normalization: "Carry the normalized translation f_delta(O) = 2^-14 F_delta(P) in captions and tables"

dimensional_check:
  benchmark_axes_and_reported_coefficients: "[dimensionless]"

approximations:
  - name: "small-delta presentation window"
    parameter: "delta <= 0.30 for the onset panels"
    validity: "the benchmark artifact is about the near-zero onset; the full-kick baseline is a separate same-family reference"
    breaks_when: "the full-kick baseline is forced onto the same narrow delta^2 axis and destroys readability"
    check: "Allow overlay or explicit juxtaposition, but require the baseline and OLE quantity to remain visibly distinct"

contract:
  schema_version: 1
  scope:
    question: "How should the exact q14 OLE data be turned into the decisive delta^2 benchmark artifact against the current perturbed_echo baseline without collapsing the small-delta axis or relabeling the baseline?"
  claims:
    - id: claim-q14-benchmark-figure
      statement: "Phase 2 can produce a decisive q14 benchmark artifact by plotting or juxtaposing the exact small-delta OLE onset curves with the existing q14 perturbed_echo baseline while keeping the observable definitions explicit."
      deliverables:
        - deliv-q14-benchmark-script
        - deliv-q14-benchmark-figure
      acceptance_tests:
        - test-benchmark-artifact
        - test-baseline-label-discipline
        - test-support-variant-visibility
      references:
        - ref-exact-ole-data
        - ref-q14-baseline-artifact
        - ref-q14-manifest
        - ref-small-delta-bridge
        - ref-phase2-handoff
  deliverables:
    - id: deliv-q14-benchmark-script
      kind: code
      path: files/quantum-math-lab/analyze_q14_ole_benchmark.py
      description: "Analysis script that reads the exact q14 OLE data and the current exact-short baseline artifact and writes the comparison report."
      must_contain:
        - "reads the exact OLE JSON and the q14 exact-short baseline JSON"
        - "produces a delta^2 comparison artifact with explicit branch labels"
        - "keeps the perturbed_echo baseline distinct from F_delta(Z_0)"
    - id: deliv-q14-benchmark-figure
      kind: figure
      path: files/quantum-math-lab/results/ole/q14_ole_vs_delta2_benchmark.md
      description: "Markdown report or figure artifact that presents the q14 small-delta OLE benchmark against the current perturbed_echo baseline."
      must_contain:
        - "delta^2 horizontal axis for the OLE onset panels"
        - "overlap G = X_0 and disjoint G = X_10 branches"
        - "explicit fixed observable definition P = Z_0 and normalized translation to f_delta(O)"
        - "a clearly labeled perturbed_echo baseline reference"
  references:
    - id: ref-exact-ole-data
      kind: prior_artifact
      locator: "files/quantum-math-lab/results/ole/black_hole_ole_q14_exact_small_delta.json"
      role: benchmark
      why_it_matters: "Supplies the exact overlap and disjoint OLE onset data that the report must present."
      applies_to:
        - claim-q14-benchmark-figure
      carry_forward_to:
        - execution
        - verification
      must_surface: true
      required_actions:
        - read
        - use
    - id: ref-q14-baseline-artifact
      kind: prior_artifact
      locator: "files/quantum-math-lab/results/benchmark/classical/black_hole_scrambling_q14_exact_short.json"
      role: benchmark
      why_it_matters: "Provides the current q14 perturbed_echo baseline that the new benchmark must compare against without renaming."
      applies_to:
        - claim-q14-benchmark-figure
      carry_forward_to:
        - execution
        - verification
      must_surface: true
      required_actions:
        - read
        - use
        - compare
    - id: ref-q14-manifest
      kind: prior_artifact
      locator: "files/quantum-math-lab/benchmarks/q14_only_manifest.json"
      role: benchmark
      why_it_matters: "Keeps the report tied to the active q14 benchmark family."
      applies_to:
        - claim-q14-benchmark-figure
      carry_forward_to:
        - execution
        - verification
      must_surface: true
      required_actions:
        - read
        - use
    - id: ref-small-delta-bridge
      kind: prior_artifact
      locator: ".gpd/phases/01/ole_small_delta_bridge.md"
      role: background
      why_it_matters: "Fixes what the small-delta onset panels mean and how the normalized translation should be carried."
      applies_to:
        - claim-q14-benchmark-figure
      carry_forward_to:
        - execution
        - verification
      must_surface: true
      required_actions:
        - read
        - use
    - id: ref-phase2-handoff
      kind: prior_artifact
      locator: ".gpd/phases/01/q14_phase2_handoff.md"
      role: benchmark
      why_it_matters: "Preserves the comparison rules, fit-window expectations, and q80/hardware guardrail language."
      applies_to:
        - claim-q14-benchmark-figure
      carry_forward_to:
        - execution
        - verification
      must_surface: true
      required_actions:
        - read
        - use
  acceptance_tests:
    - id: test-benchmark-artifact
      subject: claim-q14-benchmark-figure
      kind: benchmark
      procedure: "Check that the report presents the exact OLE onset against delta^2, carries the explicit observable definition, and includes the current q14 perturbed_echo baseline as a labeled comparison reference."
      pass_condition: "The benchmark artifact is readable, manifest-tied, and explicit about both the new OLE quantity and the older baseline quantity."
      evidence_required:
        - deliv-q14-benchmark-script
        - deliv-q14-benchmark-figure
        - ref-exact-ole-data
        - ref-q14-baseline-artifact
      automation: hybrid
    - id: test-baseline-label-discipline
      subject: claim-q14-benchmark-figure
      kind: consistency
      procedure: "Check every caption, label, and explanatory sentence that mentions the baseline to ensure it is described as perturbed_echo or state-return baseline rather than as OLE."
      pass_condition: "The report never labels the baseline curve itself as F_delta(Z_0) or as OLE."
      evidence_required:
        - deliv-q14-benchmark-figure
        - ref-q14-baseline-artifact
        - ref-small-delta-bridge
      automation: human
    - id: test-support-variant-visibility
      subject: claim-q14-benchmark-figure
      kind: benchmark
      procedure: "Check that both support branches are visible and explicitly labeled in the report."
      pass_condition: "The artifact contains both overlap G = X_0 and disjoint G = X_10 branches rather than only a single headline curve."
      evidence_required:
        - deliv-q14-benchmark-figure
        - ref-exact-ole-data
      automation: hybrid
  forbidden_proxies:
    - id: fp-baseline-renaming
      subject: claim-q14-benchmark-figure
      proxy: "Present the current perturbed_echo baseline as if it were just the delta = pi/2 point of the same OLE curve."
      reason: "The project contract requires a comparison baseline, not an identity claim."
    - id: fp-axis-collapse
      subject: claim-q14-benchmark-figure
      proxy: "Force the full-kick baseline onto the same narrow small-delta axis even if the onset panels become unreadable."
      reason: "A decisive benchmark artifact must preserve the small-delta onset information rather than bury it in presentation."
  links:
    - id: link-benchmark-script
      source: claim-q14-benchmark-figure
      target: deliv-q14-benchmark-script
      relation: supports
      verified_by:
        - test-benchmark-artifact
    - id: link-benchmark-report
      source: claim-q14-benchmark-figure
      target: deliv-q14-benchmark-figure
      relation: supports
      verified_by:
        - test-benchmark-artifact
        - test-baseline-label-discipline
        - test-support-variant-visibility
  uncertainty_markers:
    weakest_anchors:
      - "The baseline and the OLE curve live at very different kick scales, so the report may need juxtaposition rather than a literal same-axis overlay."
    disconfirming_observations:
      - "The comparison artifact cannot keep the observable semantics explicit and readable at the same time."
---

<objective>
Build the decisive q14 benchmark artifact that compares the exact small-delta OLE onset to the current q14 perturbed_echo baseline.

Purpose: Turn the exact OLE data into the report-level deliverable the project contract actually cares about.
Output: One analysis script, one benchmark report, and one benchmark-ready comparison table.
</objective>

<execution_context>
@/home/bram/.codex/get-physics-done/workflows/execute-plan.md
@/home/bram/.codex/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/REQUIREMENTS.md
@.gpd/phases/02/02-RESEARCH.md
@.gpd/phases/02/02-EXPERIMENT-DESIGN.md
@.gpd/phases/01/ole_small_delta_bridge.md
@.gpd/phases/01/q14_phase2_handoff.md
@files/quantum-math-lab/results/ole/black_hole_ole_q14_exact_small_delta.json
@files/quantum-math-lab/results/benchmark/classical/black_hole_scrambling_q14_exact_short.json
@files/quantum-math-lab/benchmarks/q14_only_manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the benchmark-report generator</name>
  <files>files/quantum-math-lab/analyze_q14_ole_benchmark.py, files/quantum-math-lab/results/ole/black_hole_ole_q14_exact_small_delta.json, files/quantum-math-lab/results/benchmark/classical/black_hole_scrambling_q14_exact_short.json</files>
  <action>Create an analysis script that reads the exact OLE JSON and the current q14 exact-short baseline artifact, organizes the data by depth and branch, and prepares a benchmark-ready comparison table.</action>
  <verify>Check that the script preserves the difference between OLE onset curves and the perturbed_echo full-kick baseline instead of flattening them into one unlabeled curve family.</verify>
  <done>The analysis script can generate the benchmark report from the exact OLE data and the baseline artifact without manual editing.</done>
</task>

<task type="auto">
  <name>Task 2: Write the q14 delta^2 benchmark report</name>
  <files>files/quantum-math-lab/analyze_q14_ole_benchmark.py, files/quantum-math-lab/results/ole/q14_ole_vs_delta2_benchmark.md</files>
  <action>Generate a report that shows `F_delta(Z_0)` versus `delta^2` for overlap and disjoint branches, carries the normalized `f_delta(O)` translation, and overlays or juxtaposes the existing q14 perturbed_echo baseline clearly and honestly.</action>
  <verify>Check that the report has a delta^2 axis, explicit branch labels, explicit observable definition, and baseline wording that never calls the baseline itself OLE.</verify>
  <done>The q14 benchmark artifact is readable, explicit, and suitable as the decisive Phase 2 figure/report.</done>
</task>

<task type="auto">
  <name>Task 3: Emit a reproducible comparison table</name>
  <files>files/quantum-math-lab/results/ole/q14_ole_vs_delta2_benchmark.csv, files/quantum-math-lab/results/ole/q14_ole_vs_delta2_benchmark.md</files>
  <action>Write a machine-readable comparison table alongside the report so the plotted or tabulated numbers can be rechecked directly during verification.</action>
  <verify>Check that the exported table is aligned to the report labels, branch names, and depth indexing.</verify>
  <done>The benchmark artifact and its comparison table are mutually consistent and reproducible.</done>
</task>

</tasks>

<verification>
- Verify that the artifact uses a delta^2 x-axis for the OLE onset panels.
- Verify that both overlap and disjoint branches are visible and labeled.
- Verify that perturbed_echo is presented only as a comparison baseline.
</verification>

<success_criteria>
- One q14 benchmark report exists and is tied to the exact OLE JSON plus the active baseline artifact.
- The report makes the observable semantics explicit and keeps the baseline distinct.
- A reproducible comparison table exists for later verification.
</success_criteria>

<output>
After completion, create `.gpd/phases/02/02-02-SUMMARY.md`.
</output>
