---
phase: "02"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - files/quantum-math-lab/qiskit_black_hole_ole_exact.py
  - files/quantum-math-lab/results/ole/black_hole_ole_q14_exact_small_delta.json
interactive: false

conventions:
  units: "dimensionless circuit observables; runtime metrics in seconds"
  metric: "euclidean bookkeeping default"
  coordinates: "Qiskit qubit indices 0..n-1"
  operator_picture: "Use the Phase 1 fixed picture A_P(U) = U P U^dagger and compute F_delta(P) first"
  observable_normalization: "Report F_delta(P) directly and carry f_delta(O) = 2^-14 F_delta(P) explicitly in the data schema"

dimensional_check:
  exact_q14_ole_outputs: "[dimensionless]"

contract:
  schema_version: 1
  scope:
    question: "How can Phase 2 compute exact q14 F_delta(Z_0) data on the active exact-short task family without dense 14-qubit operator construction or semantic drift from Phase 1?"
  claims:
    - id: claim-q14-exact-ole-data
      statement: "The q14 exact benchmark can be computed exactly by propagating the fixed Pauli observable through the existing brickwork circuit as a sparse Pauli expansion and extracting the local anti-commuting weight with respect to X_0 or X_10."
      deliverables:
        - deliv-exact-ole-script
        - deliv-exact-ole-data
      acceptance_tests:
        - test-sparse-pauli-exactness
        - test-active-manifest-disjoint-control
        - test-manifest-compatibility
      references:
        - ref-formalism-lock
        - ref-small-delta-bridge
        - ref-phase2-handoff
        - ref-q14-manifest
        - ref-scrambling-script
        - ref-q14-baseline-artifact
  deliverables:
    - id: deliv-exact-ole-script
      kind: code
      path: files/quantum-math-lab/qiskit_black_hole_ole_exact.py
      description: "Dedicated exact q14 OLE script that reuses the repo's q14 scrambler family and computes sparse-Pauli exact data for the locked overlap and disjoint branches."
      must_contain:
        - "P = Z_0, overlap G = X_0, disjoint G = X_10"
        - "exact sparse-Pauli propagation through the RX/RZ/CZ brickwork circuit or an equivalent exact Pauli-basis method"
        - "manifest-compatible depth/trial/seed handling"
        - "explicit F_delta and f_delta translation fields"
    - id: deliv-exact-ole-data
      kind: data
      path: files/quantum-math-lab/results/ole/black_hole_ole_q14_exact_small_delta.json
      description: "Exact q14 OLE benchmark data on the active manifest with branch-resolved anti-commuting weights, kappa values, and the declared delta grid."
      must_contain:
        - "active q14 manifest metadata"
        - "overlap and disjoint branch samples for depths 1,2,3,4"
        - "w_anti, kappa, F_delta, and f_delta fields"
        - "term-count or equivalent exactness bookkeeping"
  references:
    - id: ref-formalism-lock
      kind: prior_artifact
      locator: ".gpd/phases/01/ole_formalism_lock.md"
      role: must_consider
      why_it_matters: "Locks P = Z_0, G = X_0, G = X_10, and the report-level normalization rule."
      applies_to:
        - claim-q14-exact-ole-data
      carry_forward_to:
        - execution
        - verification
      must_surface: true
      required_actions:
        - read
        - use
    - id: ref-small-delta-bridge
      kind: prior_artifact
      locator: ".gpd/phases/01/ole_small_delta_bridge.md"
      role: background
      why_it_matters: "Provides the exact small-delta coefficient structure and the required F_delta versus f_delta translation."
      applies_to:
        - claim-q14-exact-ole-data
      carry_forward_to:
        - execution
        - verification
      must_surface: true
      required_actions:
        - read
        - use
        - compare
    - id: ref-phase2-handoff
      kind: prior_artifact
      locator: ".gpd/phases/01/q14_phase2_handoff.md"
      role: benchmark
      why_it_matters: "Fixes the active manifest anchor, required branches, and fit-window expectations."
      applies_to:
        - claim-q14-exact-ole-data
      carry_forward_to:
        - execution
        - verification
      must_surface: true
      required_actions:
        - read
        - use
    - id: ref-q14-manifest
      kind: prior_artifact
      locator: "files/quantum-math-lab/benchmarks/q14_only_manifest.json"
      role: benchmark
      why_it_matters: "Freezes qubits, depths, trials, seed, and baseline continuity for the q14 exact benchmark."
      applies_to:
        - claim-q14-exact-ole-data
      carry_forward_to:
        - execution
        - verification
      must_surface: true
      required_actions:
        - read
        - use
        - compare
    - id: ref-scrambling-script
      kind: prior_artifact
      locator: "files/quantum-math-lab/qiskit_black_hole_scrambling.py"
      role: method
      why_it_matters: "Defines the current brickwork scrambler family and seed discipline that Phase 2 must preserve."
      applies_to:
        - claim-q14-exact-ole-data
      carry_forward_to:
        - execution
        - verification
      must_surface: true
      required_actions:
        - read
        - compare
        - use
    - id: ref-q14-baseline-artifact
      kind: prior_artifact
      locator: "files/quantum-math-lab/results/benchmark/classical/black_hole_scrambling_q14_exact_short.json"
      role: benchmark
      why_it_matters: "Keeps the new exact OLE data tied to the active exact-short comparison family instead of drifting into a fresh task definition."
      applies_to:
        - claim-q14-exact-ole-data
      carry_forward_to:
        - execution
        - verification
      must_surface: true
      required_actions:
        - read
        - use
  acceptance_tests:
    - id: test-sparse-pauli-exactness
      subject: claim-q14-exact-ole-data
      kind: consistency
      procedure: "Check that the exact OLE script computes the evolved observable in a Pauli basis, preserves Hilbert-Schmidt norm, and records branch-resolved anti-commuting weight or an equivalent exact coefficient ledger."
      pass_condition: "The script and JSON demonstrate an exact operator-basis computation rather than a dense matrix or pure-state proxy, and the reported operator norm stays at 1 within numerical tolerance."
      evidence_required:
        - deliv-exact-ole-script
        - deliv-exact-ole-data
        - ref-small-delta-bridge
      automation: hybrid
    - id: test-active-manifest-disjoint-control
      subject: claim-q14-exact-ole-data
      kind: limiting_case
      procedure: "Verify that the disjoint control branch G = X_10 stays exactly flat on the active q14 depths 1..4 because qubit 10 lies outside the observable light cone."
      pass_condition: "The exact data record kappa = 0 and F_delta = 1 for the disjoint branch across the active manifest, up to numerical roundoff."
      evidence_required:
        - deliv-exact-ole-data
        - ref-formalism-lock
        - ref-phase2-handoff
      automation: hybrid
    - id: test-manifest-compatibility
      subject: claim-q14-exact-ole-data
      kind: benchmark
      procedure: "Check that the exact OLE output uses the active q14 qubit count, depths, trial count, and seed discipline rather than a custom benchmark matrix."
      pass_condition: "The data schema names the active manifest settings explicitly and matches the q14 exact-short family."
      evidence_required:
        - deliv-exact-ole-data
        - ref-q14-manifest
        - ref-scrambling-script
      automation: hybrid
  forbidden_proxies:
    - id: fp-pure-state-proxy
      subject: claim-q14-exact-ole-data
      proxy: "Compute only a |0...0> expectation or return probability and label it as the infinite-temperature OLE quantity."
      reason: "That would collapse the operator-space benchmark back into the state-return family the project is explicitly trying to bridge beyond."
    - id: fp-dense-operator-detour
      subject: claim-q14-exact-ole-data
      proxy: "Construct dense full-system operator matrices because q14 looks small enough."
      reason: "That path is unnecessary, brittle, and not the minimal exact method for the active light-cone-limited circuit family."
  links:
    - id: link-exact-script
      source: claim-q14-exact-ole-data
      target: deliv-exact-ole-script
      relation: supports
      verified_by:
        - test-sparse-pauli-exactness
        - test-manifest-compatibility
    - id: link-exact-data
      source: claim-q14-exact-ole-data
      target: deliv-exact-ole-data
      relation: supports
      verified_by:
        - test-sparse-pauli-exactness
        - test-active-manifest-disjoint-control
        - test-manifest-compatibility
  uncertainty_markers:
    weakest_anchors:
      - "The exact estimator is straightforward only because the active phase stays inside a narrow light cone; larger-depth or full-q80 work may require a different representation."
    disconfirming_observations:
      - "The disjoint q10 branch is not exactly flat on the active manifest."
      - "The implementation cannot compute exact q14 OLE data without falling back to dense matrices or state proxies."
---

<objective>
Implement the exact q14 OLE estimator and generate the manifest-tied exact data artifact for the locked overlap and disjoint branches.

Purpose: Put the exact q14 OLE quantity on disk before any benchmark reporting or fitting is attempted.
Output: One dedicated exact OLE script and one exact q14 JSON artifact.
</objective>

<execution_context>
@/home/bram/.codex/get-physics-done/workflows/execute-plan.md
@/home/bram/.codex/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/REQUIREMENTS.md
@.gpd/phases/02/02-RESEARCH.md
@.gpd/phases/02/02-EXPERIMENT-DESIGN.md
@.gpd/phases/01/ole_formalism_lock.md
@.gpd/phases/01/ole_small_delta_bridge.md
@.gpd/phases/01/q14_phase2_handoff.md
@files/quantum-math-lab/benchmarks/q14_only_manifest.json
@files/quantum-math-lab/qiskit_black_hole_scrambling.py
@files/quantum-math-lab/results/benchmark/classical/black_hole_scrambling_q14_exact_short.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the exact sparse-Pauli OLE evaluator</name>
  <files>files/quantum-math-lab/qiskit_black_hole_ole_exact.py, files/quantum-math-lab/qiskit_black_hole_scrambling.py</files>
  <action>Create a dedicated exact OLE script that reuses the current q14 brickwork scrambler family and seed discipline, propagates `P = Z_0` through the gate sequence in a Pauli basis, and computes branch-resolved anti-commuting weight for `G = X_0` and `G = X_10`.</action>
  <verify>Check that the implementation stays in the Pauli basis, preserves Hilbert-Schmidt norm, and never substitutes the baseline state-return quantity for the operator-space OLE quantity.</verify>
  <done>The exact OLE script can generate overlap and disjoint branch data for the active q14 manifest using the Phase 1 fixed picture and normalization.</done>
</task>

<task type="auto">
  <name>Task 2: Emit exact q14 manifest-tied OLE data</name>
  <files>files/quantum-math-lab/qiskit_black_hole_ole_exact.py, files/quantum-math-lab/results/ole/black_hole_ole_q14_exact_small_delta.json</files>
  <action>Run the exact OLE script on the active q14 manifest and write one JSON artifact containing manifest metadata, per-depth/per-trial branch data, the declared delta grid, `w_anti`, `kappa`, `F_delta`, and `f_delta`.</action>
  <verify>Check that depths `1,2,3,4`, trials `3`, seed `424242`, overlap `G = X_0`, and disjoint `G = X_10` are recorded explicitly, and that the disjoint branch is flat on the active manifest.</verify>
  <done>The exact q14 data artifact exists and is sufficient input for the benchmark report and fit-validation steps.</done>
</task>

<task type="auto">
  <name>Task 3: Record exact sanity fields for downstream validation</name>
  <files>files/quantum-math-lab/results/ole/black_hole_ole_q14_exact_small_delta.json</files>
  <action>Include enough exactness bookkeeping for later validation, such as term counts, norm checks, and any depth-0 sanity values used to confirm the estimator.</action>
  <verify>Check that downstream consumers can distinguish exact operator-basis data from a generic sampled curve.</verify>
  <done>The JSON artifact contains explicit exactness metadata and does not require reverse engineering during Phase 2 validation.</done>
</task>

</tasks>

<verification>
- Verify that the reported q14 quantity is `F_delta(P)` with explicit `f_delta(O)` translation.
- Verify that the q10 disjoint branch is exactly flat on the active manifest.
- Verify that no dense full-operator construction or state-return proxy is used.
</verification>

<success_criteria>
- A dedicated exact q14 OLE script exists and follows the Phase 1 lock.
- One exact q14 JSON artifact exists for overlap and disjoint branches on the active manifest.
- The JSON output contains enough exactness bookkeeping for later reporting and fit validation.
</success_criteria>

<output>
After completion, create `.gpd/phases/02/02-01-SUMMARY.md`.
</output>
