---
phase: "02"
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - .gpd/phases/02/q14_small_delta_validation.md
  - .gpd/phases/02/q14_phase3_handoff.md
interactive: false

conventions:
  units: "dimensionless circuit observables; runtime metrics in seconds"
  metric: "euclidean bookkeeping default"
  coordinates: "Qiskit qubit indices 0..n-1"
  operator_picture: "Validate the benchmark in the same F_delta(P) picture used in Phase 1 and 02-01/02-02"
  observable_normalization: "Judge intercepts and slopes on F_delta(P); carry f_delta(O) only as the normalized translation"

dimensional_check:
  fit_intercepts_and_quadratic_coefficients: "[dimensionless]"

approximations:
  - name: "small-delta quadratic fit"
    parameter: "fit in x = delta^2 over shrinking near-zero windows"
    validity: "the overlap branch is interpreted through the Phase 1 small-delta expansion only where intercept and slope remain stable"
    breaks_when: "quartic corrections materially move the fitted intercept or slope as the window shrinks"
    check: "require at least three nested windows and explicit stability verdicts by branch and depth"

contract:
  schema_version: 1
  scope:
    question: "Do the exact q14 benchmark curves actually support the promised small-delta interpretation, and what exact carry-forward rules should Phase 3 inherit from that result?"
  claims:
    - id: claim-q14-small-delta-validity
      statement: "Nested fit windows and exact locality controls can validate the small-delta interpretation of the q14 benchmark without confusing the comparison baseline with the OLE quantity."
      deliverables:
        - deliv-small-delta-validation-note
        - deliv-phase3-handoff-note
      acceptance_tests:
        - test-fit-window-stability
        - test-control-flatness
        - test-phase3-guardrails
      references:
        - ref-exact-ole-data
        - ref-q14-benchmark-report
        - ref-small-delta-bridge
        - ref-phase2-handoff
        - ref-status
        - ref-hardware-protocol
        - ref-levelb-evidence
  deliverables:
    - id: deliv-small-delta-validation-note
      kind: note
      path: .gpd/phases/02/q14_small_delta_validation.md
      description: "Validation note that records the nested fit windows, intercept behavior, slope stability, and control-branch verdicts for the exact q14 benchmark."
      must_contain:
        - "at least three shrinking fit windows"
        - "intercept and slope results by depth and branch"
        - "explicit pass/fail wording for the small-delta interpretation"
        - "statement that the perturbed_echo baseline is excluded from the small-delta fit"
    - id: deliv-phase3-handoff-note
      kind: note
      path: .gpd/phases/02/q14_phase3_handoff.md
      description: "Execution-facing handoff note that tells Phase 3 what exact benchmark facts are now locked and what hardware/q80 guardrails must survive."
      must_contain:
        - "what Phase 2 validated and what it did not validate"
        - "hardware-ready carry-forward paragraph tied to the local Pauli family"
        - "q80 subset/full scope guardrail"
        - "explicit warning against relabeling existing hardware outputs as OLE"
  references:
    - id: ref-exact-ole-data
      kind: prior_artifact
      locator: "files/quantum-math-lab/results/ole/black_hole_ole_q14_exact_small_delta.json"
      role: benchmark
      why_it_matters: "Provides the exact overlap/disjoint curves and fit inputs."
      applies_to:
        - claim-q14-small-delta-validity
      carry_forward_to:
        - execution
        - verification
      must_surface: true
      required_actions:
        - read
        - use
    - id: ref-q14-benchmark-report
      kind: prior_artifact
      locator: "files/quantum-math-lab/results/ole/q14_ole_vs_delta2_benchmark.md"
      role: benchmark
      why_it_matters: "Defines the report-level artifact whose interpretation is being validated."
      applies_to:
        - claim-q14-small-delta-validity
      carry_forward_to:
        - execution
        - verification
      must_surface: true
      required_actions:
        - read
        - use
    - id: ref-small-delta-bridge
      kind: prior_artifact
      locator: ".gpd/phases/01/ole_small_delta_bridge.md"
      role: background
      why_it_matters: "Supplies the fitted quantity's intended intercept and coefficient meaning."
      applies_to:
        - claim-q14-small-delta-validity
      carry_forward_to:
        - execution
        - verification
      must_surface: true
      required_actions:
        - read
        - compare
        - use
    - id: ref-phase2-handoff
      kind: prior_artifact
      locator: ".gpd/phases/01/q14_phase2_handoff.md"
      role: benchmark
      why_it_matters: "Defines the fit-window expectations and carry-forward guardrails that Phase 2 must actually satisfy."
      applies_to:
        - claim-q14-small-delta-validity
      carry_forward_to:
        - execution
        - verification
      must_surface: true
      required_actions:
        - read
        - compare
        - use
    - id: ref-status
      kind: prior_artifact
      locator: "STATUS.md"
      role: must_consider
      why_it_matters: "Keeps the q14-only claim discipline and blocks accidental large-scope overclaiming."
      applies_to:
        - claim-q14-small-delta-validity
      carry_forward_to:
        - execution
        - verification
        - writing
      must_surface: true
      required_actions:
        - read
        - use
        - avoid
    - id: ref-hardware-protocol
      kind: prior_artifact
      locator: "files/quantum-math-lab/hardware_advantage_protocol.md"
      role: method
      why_it_matters: "Constrains how Phase 3 may describe hardware continuity without overstating current evidence."
      applies_to:
        - claim-q14-small-delta-validity
      carry_forward_to:
        - execution
        - verification
        - writing
      must_surface: true
      required_actions:
        - read
        - compare
        - use
    - id: ref-levelb-evidence
      kind: prior_artifact
      locator: "files/quantum-math-lab/results/hardware/summary/levelb_evidence_report.md"
      role: must_consider
      why_it_matters: "Preserves the subset/full-q80 distinction for the next phase."
      applies_to:
        - claim-q14-small-delta-validity
      carry_forward_to:
        - execution
        - verification
        - writing
      must_surface: true
      required_actions:
        - read
        - use
        - avoid
  acceptance_tests:
    - id: test-fit-window-stability
      subject: claim-q14-small-delta-validity
      kind: benchmark
      procedure: "Fit the overlap and disjoint branches on at least three shrinking delta windows and compare the fitted intercept and slope against the Phase 1 expectations."
      pass_condition: "The overlap branch has a stable near-zero intercept and slope over the accepted windows, while the disjoint branch stays at intercept 1 and slope 0 up to numerical roundoff."
      evidence_required:
        - deliv-small-delta-validation-note
        - ref-exact-ole-data
        - ref-small-delta-bridge
      automation: hybrid
    - id: test-control-flatness
      subject: claim-q14-small-delta-validity
      kind: limiting_case
      procedure: "Check that the q10 disjoint branch is treated as an exact flat control on the active manifest and not merely as a softer comparison curve."
      pass_condition: "The validation note explicitly marks any non-flat disjoint result as an implementation failure, not as a successful physics result."
      evidence_required:
        - deliv-small-delta-validation-note
        - ref-exact-ole-data
        - ref-phase2-handoff
      automation: human
    - id: test-phase3-guardrails
      subject: deliv-phase3-handoff-note
      kind: consistency
      procedure: "Check that the Phase 3 handoff note preserves the hardware-ready local-Pauli path and the q80 subset/full split without relabeling the existing hardware outputs as OLE."
      pass_condition: "Phase 3 inherits a concrete hardware-ready direction plus explicit limitations and q80 guardrails."
      evidence_required:
        - deliv-phase3-handoff-note
        - ref-status
        - ref-hardware-protocol
        - ref-levelb-evidence
      automation: human
  forbidden_proxies:
    - id: fp-fit-everything
      subject: claim-q14-small-delta-validity
      proxy: "Include the full-kick perturbed_echo baseline point or an obviously too-large delta window in the quadratic fit and still call the fit a small-delta validation."
      reason: "That would invalidate the entire interpretation the phase is trying to establish."
    - id: fp-hardware-ready-means-done
      subject: claim-q14-small-delta-validity
      proxy: "Describe the future hardware path as already solved by the exact benchmark."
      reason: "Phase 2 only validates the exact benchmark and its carry-forward semantics, not the hardware estimator itself."
  links:
    - id: link-validation-note
      source: claim-q14-small-delta-validity
      target: deliv-small-delta-validation-note
      relation: supports
      verified_by:
        - test-fit-window-stability
        - test-control-flatness
    - id: link-phase3-handoff
      source: claim-q14-small-delta-validity
      target: deliv-phase3-handoff-note
      relation: supports
      verified_by:
        - test-phase3-guardrails
  uncertainty_markers:
    weakest_anchors:
      - "Phase 2 can validate exact small-delta behavior, but the hardware estimator choice still belongs to Phase 3."
    disconfirming_observations:
      - "The overlap-branch fit coefficients drift materially as the fit window shrinks."
      - "The Phase 3 handoff note cannot describe a hardware-ready continuity story without silently falling back to the old state-return observable."
---

<objective>
Validate the exact q14 benchmark's small-delta interpretation and write the carry-forward note for Phase 3.

Purpose: Close the gap between "we have exact curves" and "those curves satisfy the promised Phase 2 claim."
Output: One validation note and one Phase 3 handoff note.
</objective>

<execution_context>
@/home/bram/.codex/get-physics-done/workflows/execute-plan.md
@/home/bram/.codex/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/REQUIREMENTS.md
@.gpd/phases/02/02-RESEARCH.md
@.gpd/phases/02/02-EXPERIMENT-DESIGN.md
@.gpd/phases/01/ole_small_delta_bridge.md
@.gpd/phases/01/q14_phase2_handoff.md
@STATUS.md
@files/quantum-math-lab/results/ole/black_hole_ole_q14_exact_small_delta.json
@files/quantum-math-lab/results/ole/q14_ole_vs_delta2_benchmark.md
@files/quantum-math-lab/hardware_advantage_protocol.md
@files/quantum-math-lab/results/hardware/summary/levelb_evidence_report.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the nested fit-window validation</name>
  <files>.gpd/phases/02/q14_small_delta_validation.md, files/quantum-math-lab/results/ole/black_hole_ole_q14_exact_small_delta.json</files>
  <action>Fit the overlap and disjoint exact curves on at least three shrinking delta windows, record fitted intercepts and slopes by depth and branch, and mark which windows count as the accepted small-delta regime.</action>
  <verify>Check that the disjoint branch remains exactly flat, the overlap intercept stays near 1, and the accepted slope does not drift materially under shrinking windows.</verify>
  <done>The validation note contains a branch-by-branch, depth-by-depth stability verdict rather than only raw fit numbers.</done>
</task>

<task type="auto">
  <name>Task 2: State exactly what the benchmark does and does not prove</name>
  <files>.gpd/phases/02/q14_small_delta_validation.md, files/quantum-math-lab/results/ole/q14_ole_vs_delta2_benchmark.md</files>
  <action>Write explicit conclusion text that distinguishes the validated exact q14 OLE onset from the separate perturbed_echo baseline and says what is still deferred to hardware translation.</action>
  <verify>Check that the validation note never treats the baseline as fit input or as the same observable.</verify>
  <done>The note closes the interpretation gap between the report and the Phase 2 contract.</done>
</task>

<task type="auto">
  <name>Task 3: Write the Phase 3 handoff note</name>
  <files>.gpd/phases/02/q14_phase3_handoff.md, files/quantum-math-lab/hardware_advantage_protocol.md, files/quantum-math-lab/results/hardware/summary/levelb_evidence_report.md</files>
  <action>Summarize the exact benchmark facts Phase 3 must inherit, describe the hardware-ready local-Pauli carry-forward path, and keep the q80 subset/full distinction explicit.</action>
  <verify>Check that the note gives Phase 3 a concrete continuity story without implying the hardware estimator is already implemented or that subset-only q80 evidence settles the full-q80 question.</verify>
  <done>The next phase can start from a concrete handoff note instead of reopening Phase 2 semantics.</done>
</task>

</tasks>

<verification>
- Verify the overlap branch with shrinking fit windows in delta^2.
- Verify the q10 disjoint branch stays flat and is treated as an exact control.
- Verify the Phase 3 handoff note preserves hardware-ready and q80 scope guardrails.
</verification>

<success_criteria>
- A validation note exists and states which exact q14 curves support the small-delta interpretation.
- The disjoint branch is handled as an exact control, not merely as a suggestive comparison.
- A Phase 3 handoff note exists with concrete hardware/q80 carry-forward language.
</success_criteria>

<output>
After completion, create `.gpd/phases/02/02-03-SUMMARY.md`.
</output>
