---
phase: "03"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .gpd/research/OLE_HARDWARE_PATH.md
interactive: false
conventions:
  units: "dimensionless observables; runtime in IBM quantum_seconds"
  metric: "euclidean"
  coordinates: "qubit index ordering 0..n-1"
  picture: "operator-picture AP form A_P(U)=U P U^dagger"
dimensional_check:
  f_delta_O: "[dimensionless]"
  perturbed_echo_ratio: "[dimensionless]"
  runtime_metric: "[time]"
approximations:
  - name: "Fixed-observable continuity map"
    parameter: "manifest-locked q14 regime only"
    validity: "P=Z_0, O=Z_0/sqrt(2^14), perturbation branches q=0 and q=10, depths 1,2,3,4"
    breaks_when: "Any change to manifest values, or relabeling perturbed_echo as the OLE estimator"
    check: "Manifest drift check and explicit no-proxy section in the note"
contract:
  schema_version: 1
  scope:
    question: "How can we map the fixed-observable q14 OLE benchmark onto the existing hardware pipeline as a hardware-ready execution contract without turning perturbed_echo into a relabeled OLE estimator?"
  claims:
    - id: claim-q14-ole-bridge
      statement: "A hardware mapping note explicitly maps the q14 OLE definition and branch definitions to `qiskit_black_hole_hardware_runner.py` command/result semantics while preserving manifest locks and fixed-observable scope."
      deliverables: [deliv-hardware-ready-note]
      acceptance_tests: [test-hardware-ready]
      references: [ref-algorithmiq-ole, ref-status, ref-q14-manifest, manifest-levelc-v1, protocol-levelc-v1, ibm-raw-vs-mit]
  deliverables:
    - id: deliv-hardware-ready-note
      kind: note
      path: .gpd/research/OLE_HARDWARE_PATH.md
      description: "Phase 3 hardware-ready mapping note for q14 fixed-observable OLE continuity and explicit non-equivalence of hardware baselines"
      must_contain:
        - "Explicit fixed-observable bridge with P=Z_0, O=Z_0/sqrt(2^14), and branch labels q=0, q=10"
        - "Manifest lock table (qubits=14, depths=1,2,3,4, trials=3, seed=424242, shots=4000)"
        - "Command-to-output matrix for raw/mitigated variants"
        - "Explicit statement that current hardware outputs are baseline-compatible, not final OLE estimator outputs"
  references:
    - id: ref-algorithmiq-ole
      kind: paper
      locator: "Algorithmiq OLE formalism reference (1903.02651v4 and related notes in project guidance)"
      role: definition
      why_it_matters: "Defines fixed-observable OLE and small-delta form to preserve contract discipline"
      applies_to: [claim-q14-ole-bridge]
      carry_forward_to: [planning, verification, writing]
      must_surface: true
      required_actions: [read, compare, cite]
      aliases: [ref-algorithmiq-ole]
    - id: ref-status
      kind: user_anchor
      locator: STATUS.md
      role: must_consider
      why_it_matters: "Constrains permissible claim language, prevents OLE overclaim, and keeps scope boundaries explicit"
      applies_to: [claim-q14-ole-bridge]
      carry_forward_to: [planning, verification, writing]
      must_surface: true
      required_actions: [read, use, avoid]
      aliases: [ref-status]
    - id: ref-q14-manifest
      kind: prior_artifact
      locator: files/quantum-math-lab/benchmarks/q14_only_manifest.json
      role: benchmark
      why_it_matters: "Freezes q14 depth/trial/seed/delta family compatibility into every mapped hardware call"
      applies_to: [claim-q14-ole-bridge]
      carry_forward_to: [execution, verification]
      must_surface: true
      required_actions: [read, use, compare]
      aliases: [ref-q14-manifest]
    - id: ref-levelb-evidence
      kind: prior_artifact
      locator: files/quantum-math-lab/results/hardware/summary/levelb_evidence_report.md
      role: background
      why_it_matters: "Reminds the mapping to keep q80 subset/full distinction and avoid global claims from subset data"
      applies_to: [claim-q14-ole-bridge]
      carry_forward_to: [planning, verification]
      must_surface: false
      required_actions: [read, avoid]
      aliases: [ref-levelb-evidence]
    - id: manifest-levelc-v1
      kind: prior_artifact
      locator: files/quantum-math-lab/benchmarks/levelc_manifest.json
      role: benchmark
      why_it_matters: "Defines the legacy level-C manifest family and runtime/size criteria relevant to hardware-readiness guardrails."
      applies_to: [claim-q14-ole-bridge]
      carry_forward_to: [planning, execution, verification, writing]
      must_surface: true
      required_actions: [use, compare]
      aliases: [manifest-levelc-v1]
    - id: protocol-levelc-v1
      kind: benchmark
      locator: files/quantum-math-lab/hardware_advantage_protocol.md
      role: benchmark
      why_it_matters: "Defines anti-overclaim and pass/fail rules used by this phase's hardware mapping contract."
      applies_to: [claim-q14-ole-bridge]
      carry_forward_to: [planning, execution, verification, writing]
      must_surface: true
      required_actions: [read, compare]
      aliases: [protocol-levelc-v1]
    - id: ibm-raw-vs-mit
      kind: prior_artifact
      locator: files/quantum-math-lab/results/hardware/summary/ibm_runs_raw_vs_mitigated_summary.md
      role: benchmark
      why_it_matters: "Provides raw-vs-mitigated per-depth comparison behavior needed for gap and re-claim controls."
      applies_to: [claim-q14-ole-bridge]
      carry_forward_to: [planning, verification, writing]
      must_surface: true
      required_actions: [read, compare]
      aliases: [ibm-raw-vs-mit]
  acceptance_tests:
    - id: test-hardware-ready
      subject: claim-q14-ole-bridge
      kind: reproducibility
      procedure: "Verify the note exists and maps the fixed-observable hardware-ready pipeline with concrete command/output fields, manifest lock values, and explicit non-estimator limitations."
      pass_condition: "All required controls (`P`, `q`, `subset`, `shots`, `depths`, `trials`, `seed`) resolve to documented fields in the runner; manifest-lock values are unchanged; unresolved-gap and anti-overclaim blocks remain explicit."
      evidence_required: [deliv-hardware-ready-note, ref-q14-manifest, ref-status, manifest-levelc-v1, protocol-levelc-v1, ibm-raw-vs-mit]
      automation: hybrid
  forbidden_proxies:
    - id: fp-03-01-proxy
      subject: claim-q14-ole-bridge
      proxy: "Accepting the q14 hardware runner command sheet as a completed OLE estimator because it reproduces perturbed_echo curves"
      reason: "Phase 3 only gains hardware-ready mapping evidence until a fixed-observable circuit-level estimator is explicitly defined and validated"
  links:
    - id: link-03-01-note
      source: claim-q14-ole-bridge
      target: deliv-hardware-ready-note
      relation: benchmarks
      verified_by: [test-hardware-ready]
    - id: link-03-01-gap
      source: claim-q14-ole-bridge
      target: deliv-hardware-ready-note
      relation: supports
      verified_by: [test-hardware-ready]
  uncertainty_markers:
    weakest_anchors: ["external OLE estimator replacement path beyond baseline-compatible runner outputs"]
    disconfirming_observations: ["A q14 hardware mapping note that appears equivalent to OLE without explicit estimator rewrite", "manifest drift from q14_only_manifest.json"]
---

<objective>
Create the first Phase 3 deliverable: a hardware-ready mapping note that preserves the fixed-observable q14 bridge while keeping estimator semantics explicit.

Purpose: make the 14-qubit q14 OLE continuation executable by contract, without claiming an implemented OLE estimator before it exists.
Output: `.gpd/research/OLE_HARDWARE_PATH.md` and a clear dependency list for later 80q scope work.
</objective>

<execution_context>
@/home/bram/.codex/get-physics-done/workflows/execute-plan.md
@/home/bram/.codex/get-physics-done/templates/summary.md
@/home/bram/.codex/get-physics-done/references/verification/core/verification-core.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/research-map/REFERENCES.md
@.gpd/phases/03/03-RESEARCH.md
@.gpd/phases/03/03-EXPERIMENT-DESIGN.md
@files/quantum-math-lab/qiskit_black_hole_hardware_runner.py
@files/quantum-math-lab/qiskit_black_hole_ole_exact.py
@files/quantum-math-lab/benchmarks/q14_only_manifest.json
@files/quantum-math-lab/results/ole/q14_ole_vs_delta2_benchmark.csv
</context>

<tasks>

<task type="auto">
  <name>Task 01: Anchor-guarded mapping contract</name>
  <files>.gpd/research/OLE_HARDWARE_PATH.md, files/quantum-math-lab/benchmarks/q14_only_manifest.json</files>
  <action>Extract locked observables and control fields from `.gpd/PROJECT.md`, `.gpd/STATE.md`, and `q14_only_manifest.json`, then write the fixed-observable bridge definition (`F_delta(P)`, `f_delta(O)`, `P=Z_0`, `O=Z_0/sqrt(2^14)`) and manifest lock block for q14 (`qubits=14`, depths `1,2,3,4`, trials `3`, seed `424242`). Include branch labels `q=0` and `q=10`, raw-vs-mitigated columns, and output schema fields expected from the existing runner.</action>
  <verify>Dimensional and consistency check: `F_delta(P)` and `f_delta(O)` are dimensionless, branch labels are explicit, and manifest values are numerically identical to manifest JSON.</verify>
  <done>Note section contains a complete control-field mapping and no changed manifest value relative to the locked handoff contract.</done>
</task>

<task type="auto">
  <name>Task 02: Command-to-output matrix and estimator-gap statement</name>
  <files>.gpd/research/OLE_HARDWARE_PATH.md, files/quantum-math-lab/qiskit_black_hole_hardware_runner.py</files>
  <action>Construct a command-output map for q14 overlap (`perturb-qubit=0`) and disjoint (`perturb-qubit=10`) branches in both raw and mitigated mode, including the fixed command template for 4000 shots and 6000 calibration shots where applicable. Add a hard anti-overclaim clause that explicitly states existing `perturbed_echo` and `perturbed_subset_echo` are same-family baselines only and cannot be treated as final OLE estimator outputs without additional explicit circuit rewrite.</action>
  <verify>Cross-check that each command row resolves to documented fields in `qiskit_black_hole_hardware_runner.py`; confirm every row has source input/output paths and a corresponding branch semantics description.</verify>
  <done>The matrix is complete, internally consistent, and contains the unresolved-gap section with explicit conditions for OLE-equivalent claims.</done>
</task>

<task type="auto">
  <name>Task 03: O(δ) stability and transition checks before handoff</name>
  <files>.gpd/research/OLE_HARDWARE_PATH.md, .gpd/phases/03/03-EXPERIMENT-DESIGN.md</files>
  <action>Add order-of-magnitude and error-budget guardrails before execution handoff: nominal shot burden estimates (`96k` shots per q14 branch class at 4000 shots), mitigation overhead, and trial-noise interpretation (`3` trials fixed spread reporting). Include a pre-handoff skeptical check that disqualifies progression to estimator validation unless disjoint branch flatness and small-δ labeling are preserved.</action>
  <verify>Limiting-case checks must include the q14 overlap small-δ convention (`delta<=0.10` quoted, `delta<=0.20` broader diagnostic) and depth-0 sanity condition (`kappa≈4` for overlap and flat behavior for disjoint).</action>
  <done>The note includes explicit error-budget assumptions, shot-load scaling, and at least one disconfirming-signal condition that blocks false OLE claims.</done>
</task>

</tasks>

<verification>
Plan checks must include:
- Dimensional consistency of `f_delta(O)` and `F_delta(P)` in all stated equations.
- Tensor and branch-structure consistency between `P`,`G`, and perturb-control points (`q=0`, `q=10`) against prior decisions.
- Symmetry/picture-consistency check that every estimator statement is tagged as fixed-observable OLE-compatible or baseline-only.
- Limiting-case check (`delta`-window conventions, depth-0 curvature expectations).
- Reproducibility check that manifest values are frozen and all output paths are versioned.
</verification>

<success_criteria>
- [ ] `.gpd/research/OLE_HARDWARE_PATH.md` exists and contains a completed hardware mapping matrix.
- [ ] Manifest lock and branch table are complete and consistent with `q14_only_manifest.json`.
- [ ] Note contains an explicit no-relabel statement for perturbed_echo as a baseline-only quantity.
- [ ] The unresolved estimator-gap block is present and blocks any full-claim interpretation without additional circuit-level implementation.
- [ ] Acceptance test `test-hardware-ready` is satisfied.
- [ ] The note preserves q80 scope discipline language (`levelb` continuity without full-q80 equivalence claims).
</success_criteria>

<output>
After completion, create `.gpd/research/OLE_HARDWARE_PATH.md` and include a machine-consumable command matrix block plus unresolved-estimator gate.
</output>

## Execution-status addendum (2026-03-19)

This plan's deliverable has now been executed far enough to set a concrete interpretation boundary.

Execution verdict:

- `.gpd/research/OLE_HARDWARE_PATH.md` is complete and now contains the q14 hardware mapping outcome, corrected local-fold checkpoint ZNE update, and PE-LiNN mitigation verdict.
- q14 hardware mapping is successful as a contract-preserving execution path.
- Corrected checkpoint ZNE is admissible as branch-controlled checkpoint evidence.
- PE-LiNN mitigation is not admissible for claim support on the current q14 dataset because validation error remains significantly worse than the existing mitigated baseline.

Practical carry-forward rule:

- Use readout mitigation plus corrected checkpoint ZNE as the strongest allowed q14 hardware evidence in later phase-3 writing.
- Do not use PE-LiNN outputs as supporting evidence for q14 or q80 claim language.
- Keep all later wording at `hardware-ready mapping + checkpoint evidence`, not `full OLE-equivalent hardware confirmation`.
