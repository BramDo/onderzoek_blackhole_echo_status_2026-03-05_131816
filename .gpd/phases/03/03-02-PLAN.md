---
phase: "03"
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - .gpd/research/Q80_SCOPE.md
interactive: false
conventions:
  units: "dimensionless observables; runtime in IBM quantum_seconds"
  metric: "euclidean"
  coordinates: "fixed subset qubit labels"
  gauge: "none (finite-depth gate model benchmark)"
dimensional_check:
  f_delta_O: "[dimensionless]"
  subset_echo: "[dimensionless]"
  depth_index: "[dimensionless]"
approximations:
  - name: "Subset continuation heuristic"
    parameter: "fixed-support q80 subset observables only (S_A, S_B)"
    validity: "works for locality-trace hardware continuation while full-q80 OLE remains unresolved"
    error_tolerance: "require absolute depth-wise overlap/disjoint contrast >= 1.5e-3 for at least 2 of 4 depths"
    reevaluation_threshold: "pause and re-derive continuation design if either (a) raw-vs-mitigated spread exceeds 25% at matching depth or (b) contrast changes sign in 2 or more consecutive depths"
    breaks_when: "subset supports are repurposed as global overlap estimators"
    check: "full-q80 guardrail in the scope note and independent disjoint/overlap contrast table"
contract:
  schema_version: 1
  scope:
    question: "How can the immediate q80 route remain explicit subset-observable continuation while retaining visibility of the unresolved full-q80 fixed-observable OLE question?"
  claims:
    - id: claim-q80-visibility
      statement: "The q80 continuation note fixes immediate subset-observable continuation to two supports (`S_A=0..9`, `S_B=10..19`) with overlap/disjoint branch semantics, while explicitly preserving full-q80 as an unresolved follow-on direction."
      deliverables: [deliv-q80-scope-note]
      acceptance_tests: [test-q80-scope-guard]
      references: [ref-levelb-evidence, ref-status, manifest-levelc-v1, protocol-levelc-v1, ibm-raw-vs-mit, ref-q14-manifest, ref-algorithmiq-ole]
  deliverables:
    - id: deliv-q80-scope-note
      kind: note
      path: .gpd/research/Q80_SCOPE.md
      description: "q80 continuation scope note with explicit subset labels, branch contrast table, raw-vs-mitigated error strategy, and unresolved full-q80 section"
      must_contain:
        - "Explicitly fixed subsets S_A=0..9 and S_B=10..19"
        - "Overlap and disjoint branch rows for each subset and depth"
        - "Raw vs readout-mitigated pair tables with spread/error handling"
        - "Explicit unresolved full-q80 statement and fallback condition"
  references:
    - id: ref-levelb-evidence
      kind: prior_artifact
      locator: files/quantum-math-lab/results/hardware/summary/levelb_evidence_report.md
      role: must_consider
      why_it_matters: "Defines accepted subset-locality interpretation and prevents full-q80 overclaim"
      applies_to: [claim-q80-visibility]
      carry_forward_to: [planning, verification, writing]
      must_surface: true
      required_actions: [read, use, avoid]
      aliases: [ref-levelb-evidence]
    - id: ref-status
      kind: user_anchor
      locator: STATUS.md
      role: must_consider
      why_it_matters: "Constrains q80 scope language so subset observables are never presented as full global OLE"
      applies_to: [claim-q80-visibility]
      carry_forward_to: [planning, writing]
      must_surface: true
      required_actions: [read, use, avoid]
      aliases: [ref-status]
    - id: ref-q14-manifest
      kind: prior_artifact
      locator: files/quantum-math-lab/benchmarks/q14_only_manifest.json
      role: background
      why_it_matters: "Keeps phase-3 continuity metrics and naming conventions coherent when scaling to q80 subset workflow"
      applies_to: [claim-q80-visibility]
      carry_forward_to: [execution]
      must_surface: false
      required_actions: [read, compare]
      aliases: [ref-q14-manifest]
    - id: ref-algorithmiq-ole
      kind: paper
      locator: "Algorithmiq OLE theory note used as fixed-observable definition source"
      role: method
      why_it_matters: "Ensures q80 scope remains tied to fixed-observable OLE interpretation and not state-overlap relabeling"
      applies_to: [claim-q80-visibility]
      carry_forward_to: [planning, execution]
      must_surface: false
      required_actions: [read, compare]
      aliases: [ref-algorithmiq-ole]
    - id: manifest-levelc-v1
      kind: prior_artifact
      locator: files/quantum-math-lab/benchmarks/levelc_manifest.json
      role: benchmark
      why_it_matters: "Keeps the q80 subset continuation anchored to legacy level-C manifest scope and continuity expectations."
      applies_to: [claim-q80-visibility]
      carry_forward_to: [planning, execution, verification]
      must_surface: true
      required_actions: [use, compare]
      aliases: [manifest-levelc-v1]
    - id: protocol-levelc-v1
      kind: benchmark
      locator: files/quantum-math-lab/hardware_advantage_protocol.md
      role: benchmark
      why_it_matters: "Supplies pass criteria and anti-overclaim constraints for subset hardware continuation."
      applies_to: [claim-q80-visibility]
      carry_forward_to: [planning, verification, writing]
      must_surface: true
      required_actions: [read, compare]
      aliases: [protocol-levelc-v1]
    - id: ibm-raw-vs-mit
      kind: prior_artifact
      locator: files/quantum-math-lab/results/hardware/summary/ibm_runs_raw_vs_mitigated_summary.md
      role: benchmark
      why_it_matters: "Provides raw-vs-mitigated spread and diagnostic behavior expected in q80 subset continuity notes."
      applies_to: [claim-q80-visibility]
      carry_forward_to: [verification, writing]
      must_surface: true
      required_actions: [read, compare]
      aliases: [ibm-raw-vs-mit]
  acceptance_tests:
    - id: test-q80-scope-guard
      subject: claim-q80-visibility
      kind: human_review
      procedure: "Check deliv-q80-scope-note has explicit subset labels and fixed controls (`q=0`, `q=10`), raw/mitigated branch tables, and an unresolved full-q80 block."
      pass_condition: "Both S_A and S_B are present with manifest-like execution metadata, at least one depth-wise overlap-disjoint contrast metric is reported, and subset scope is never presented as final full-q80 evidence."
      evidence_required: [deliv-q80-scope-note, ref-levelb-evidence, ref-status]
      automation: human
  forbidden_proxies:
    - id: fp-03-02-subset-as-full
      subject: claim-q80-visibility
      proxy: "Treating q80 subset overlap/disjoint signals as a proof of full-q80 fixed-observable OLE"
      reason: "Subset evidence is locality-utility evidence; contract requires this to remain explicit."
  links:
    - id: link-03-02-scope
      source: claim-q80-visibility
      target: deliv-q80-scope-note
      relation: supports
      verified_by: [test-q80-scope-guard]
    - id: link-03-02-open
      source: claim-q80-visibility
      target: deliv-q80-scope-note
      relation: depends_on
      verified_by: [test-q80-scope-guard]
  uncertainty_markers:
    weakest_anchors: ["stability of q80 subset-to-full-q80 extrapolation under future hardware regimes"]
    disconfirming_observations: ["if overlap/disjoint contrast disappears in rerun, or if raw-vs-mitigated does not remain support-labeled"]
</contract>

<objective>
Define the immediate q80 continuation as explicit subset-observable hardware scope with fixed supports and branch controls, while preserving the unresolved full-q80 question as an explicit open item.

Purpose: prevent overclaim drift and create a review-ready scope note that can be executed and validated independently.
Output: `.gpd/research/Q80_SCOPE.md` with subset layout, branch contrast table, and full-q80 guardrail.
</objective>

<execution_context>
@/home/bram/.codex/get-physics-done/workflows/execute-plan.md
@/home/bram/.codex/get-physics-done/templates/summary.md
@/home/bram/.codex/get-physics-done/references/verification/core/verification-core.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/research-map/REFERENCES.md
@.gpd/phases/03/03-RESEARCH.md
@.gpd/phases/03/03-EXPERIMENT-DESIGN.md
@files/quantum-math-lab/results/hardware/summary/levelb_evidence_report.md
@files/quantum-math-lab/results/hardware/summary/ibm_runs_raw_vs_mitigated_summary.md
@files/quantum-math-lab/results/hardware/black_hole_hardware_q80_subset01_mit.json
@files/quantum-math-lab/results/hardware/black_hole_hardware_q80_subset02_mit_xsup_pq10_rerun1.json
</context>

<tasks>

<task type="auto">
  <name>Task 01: Define fixed subset and branch matrix</name>
  <files>.gpd/research/Q80_SCOPE.md, files/quantum-math-lab/results/hardware/summary/levelb_evidence_report.md</files>
  <action>Create a canonical q80 plan skeleton with `S_A=0..9` and `S_B=10..19`, both perturb controls `q=0` and `q=10`, and fixed 80q execution conventions (`trials=3`, `seed=424242`, `shots=4000`, depths `1..4`). Record order-of-magnitude expectations before any detailed script execution: `24` configs per subset/branch class and baseline shot scale from 14q to q80 continuity.</action>
  <verify>Consistency check against contract: every subset branch entry must include subset support, perturb branch, and raw/mitigated columns; labels must remain subset-observable language.</verify>
  <done>A complete template matrix exists for all 4 subset/branch combinations with manifest-locked execution metadata.</done>
</task>

<task type="auto">
  <name>Task 02: Document locality contrast and reproducibility gates</name>
  <files>.gpd/research/Q80_SCOPE.md, files/quantum-math-lab/results/hardware/summary/ibm_runs_raw_vs_mitigated_summary.md</files>
  <action>Populate a depth-wise overlap-vs-disjoint contrast table for at least one planned subset family, with raw vs mitigated fields and explicit error-interpretation rules. Specify convergence diagnostics (e.g., 4000→8000 shots on a checkpoint branch and 3→5 trials for a rerun check) as diagnostic-only, never mixed into final scope claims.</action>
  <verify>Limiting-case check for the q80 table: disjoint-control rows should remain the locality-control comparison baseline; overlap rows should carry explicit support-correlation interpretation.</verify>
  <done>The table identifies which differences are support-locality signal versus noise and flags required rerun behavior.</done>
</task>

<task type="auto">
  <name>Task 03: Add explicit full-q80 visibility and open-question block</name>
  <files>.gpd/research/Q80_SCOPE.md</files>
  <action>Write a non-negotiable full-q80 block with: (i) what is closed now (subset scope), (ii) what remains unresolved, (iii) the criteria that would trigger a future full-q80 estimator design, and (iv) the explicit anti-overclaim sentence matching PROJECT/STATUS wording.</action>
  <verify>Check that the note never uses full-q80 language to describe completed subset results and that rejected proxy path is visible in text.</verify>
  <done>The note contains a clearly labeled unresolved full-q80 section and a testable statement that subset evidence is not the final target.</done>
</task>

</tasks>

<verification>
Plan-level verification:
- Correctness checks: subset labels, branch labels, and support fields are consistent across all output tables.
- Dimensional checks: all observables and echo-like fields are dimensionless; runtime values remain time-axis compatible.
- Limiting checks: subset/locality contrast is interpreted for both overlap and disjoint perturbations and compared against the prior Level-B evidence style.
- Error-budget checks: shot-depth and trial-variation spread are recorded before claiming trends.
- Cross-method consistency: compare any q80 diagnostic plan against `levelb_evidence_report.md` and `ibm_runs_raw_vs_mitigated_summary.md` for interpretation consistency.
</verification>

<success_criteria>
- [ ] `.gpd/research/Q80_SCOPE.md` exists and contains both fixed subset and branch-labeled execution plans.
- [ ] Disjoint/overlap interpretation is explicit per depth and not presented as a global OLE claim.
- [ ] Raw/mitigated field schema is explicit and includes subset labels for every row.
- [ ] A rerun/reproducibility plan or spread column is included to support convergence interpretation.
- [ ] Explicit `full-q80 unresolved` block is present and linked to future follow-up criteria.
- [ ] Acceptance test `test-q80-scope-guard` is satisfied before gating downstream.
</success_criteria>

<output>
After completion, create `.gpd/research/Q80_SCOPE.md` with a reproducibility-safe summary block and subset-locality interpretation matrix.
</output>

## Execution-status addendum (2026-03-19)

The downstream q80 scope must inherit the completed q14 hardware verdict boundary.

Carry-forward verdict:

- corrected local-fold checkpoint ZNE is the strongest admissible q14 checkpoint evidence
- PE-LiNN mitigation is not admissible claim support on the current q14 dataset
- later q80 notes must remain subset-locality notes, not full-q80 confirmation notes

Implementation consequence:

- use `.gpd/research/Q80_SCOPE.md` as the q80 wording boundary note
- do not route q80 support language through PE-LiNN artifacts
- keep later writing at `subset-locality evidence under the q14 hardware interpretation boundary`

## Seeded control addendum (2026-03-20)

Execution should treat the seeded q20 subset-proxy control set as the phase-03.02 reference:
- S_A primary comparison: q=0 versus q=19.
- S_B primary comparison: q=10 versus q=0.
- S_A q=10 remains diagnostic-only and should not be used as the sole disjoint comparator.

Decision boundary:
- Main tables and summaries should foreground the seeded far-disjoint contrasts at depths 1 and 2.
- Any q80 subset-locality wording should point back to the seeded far-disjoint control pair, not the earlier near-boundary S_A q=10 comparison.

## q80 pilot milestone addendum (2026-03-20)

The initial q80 pilot milestone is now satisfied by the completed `S_A = 0..9` far-disjoint pilot:
- overlap `q=0`
- far-disjoint `q=79`
- seeded shallow-depth contrasts: depth 1 `+0.98420`, depth 2 `+0.89110`

Execution consequence:
- Phase 03.02 can now treat the first q80 subset-proxy pilot as complete.
- A symmetric second-subset pilot remains valuable follow-up, but it is no longer required to record the successful q80 scale-up milestone.
- Downstream writing must continue to distinguish `successful first q80 subset pilot` from `symmetric q80 validation` and from any blocked full-q80 claim.
