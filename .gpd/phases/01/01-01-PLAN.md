---
phase: "01"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .gpd/phases/01/ole_formalism_lock.md
  - .gpd/CONVENTIONS.md
  - .gpd/PROJECT.md
  - files/quantum-math-lab/README.md
interactive: false

conventions:
  units: "dimensionless circuit observables; runtime metrics in seconds"
  metric: "euclidean bookkeeping default"
  coordinates: "Qiskit qubit indices 0..n-1"
  operator_picture: "Define OLE through A = U P U^dagger and translate other pictures explicitly"
  observable_normalization: "Write both P with Tr(P^2)=2^n and O = P/sqrt(2^n) with Tr(O^2)=1"

dimensional_check:
  reported_ole_and_baseline_quantities: "[dimensionless]"

contract:
  schema_version: 1
  scope:
    question: "What fixed observable, generator, support layout, and normalization convention will define the first q14 OLE benchmark without collapsing back into perturbed_echo semantics?"
  claims:
    - id: claim-q14-ole-bridge
      statement: "The first q14 OLE benchmark is anchored to the explicit Pauli observable P = Z_0, overlap generator G = X_0, disjoint control G = X_10, and a written normalization rule that fixes the delta=0 intercept."
      deliverables:
        - deliv-formalism-lock-note
        - deliv-convention-ledger-update
      acceptance_tests:
        - test-formalism-lock
        - test-normalization-intercept
      references:
        - ref-algorithmiq-ole
        - ref-status
        - ref-q14-manifest
        - ref-scrambling-script
  deliverables:
    - id: deliv-formalism-lock-note
      kind: note
      path: .gpd/phases/01/ole_formalism_lock.md
      description: "Formal note that freezes the first q14 observable/generator/support choices and the reporting convention for the plotted OLE curve."
      must_contain:
        - "Default fixed observable P = Z_0 and normalized operator O = Z_0 / sqrt(2^14)"
        - "Overlap generator G = X_0 and disjoint control G = X_10"
        - "Support labels and qubit-indexing assumptions"
        - "Explicit delta=0 intercept for the chosen reporting convention"
    - id: deliv-convention-ledger-update
      kind: note
      path: .gpd/CONVENTIONS.md
      description: "Updated convention ledger entry that records the Phase 1 OLE normalization, picture choice, and support-label guardrails."
      must_contain:
        - "Observable normalization rule for the first OLE benchmark"
        - "Picture choice expressed in one fixed operator convention"
        - "No-relabeling guardrail for perturbed_echo"
  references:
    - id: ref-algorithmiq-ole
      kind: spec
      locator: "Algorithmiq OLE theory note"
      aliases:
        - "OLE theory PDF"
      role: definition
      why_it_matters: "Provides the fixed-observable definition and small-delta bridge that the project must freeze without drift."
      applies_to:
        - claim-q14-ole-bridge
      carry_forward_to:
        - execution
        - verification
      must_surface: true
      required_actions:
        - read
        - compare
        - cite
    - id: ref-status
      kind: prior_artifact
      locator: "STATUS.md"
      role: must_consider
      why_it_matters: "Preserves the q14-only claim language and blocks subset/full-q80 overclaiming."
      applies_to:
        - claim-q14-ole-bridge
      carry_forward_to:
        - execution
        - verification
      must_surface: true
      required_actions:
        - read
        - use
        - avoid
    - id: ref-q14-manifest
      kind: prior_artifact
      locator: "files/quantum-math-lab/benchmarks/q14_only_manifest.json"
      role: benchmark
      why_it_matters: "Freezes the q14 task family and available qubit indices."
      applies_to:
        - claim-q14-ole-bridge
      carry_forward_to:
        - execution
        - verification
      must_surface: true
      required_actions:
        - read
        - use
        - compare
    - id: ref-scrambling-script
      kind: prior_artifact
      locator: "files/quantum-math-lab/qiskit_black_hole_scrambling.py"
      role: method
      why_it_matters: "Defines the current X-kick baseline semantics that the OLE bridge must reuse without renaming."
      applies_to:
        - claim-q14-ole-bridge
      carry_forward_to:
        - execution
        - verification
      must_surface: true
      required_actions:
        - read
        - compare
  acceptance_tests:
    - id: test-formalism-lock
      subject: claim-q14-ole-bridge
      kind: consistency
      procedure: "Check that the formalism note explicitly names P = Z_0, overlap G = X_0, disjoint G = X_10, and support labels compatible with q14 qubit indices 0..13 and the existing X-kick implementation."
      pass_condition: "All named objects and support labels appear explicitly, and none require a script-level observable definition different from the current q14 manifest or X-kick machinery."
      evidence_required:
        - deliv-formalism-lock-note
        - ref-q14-manifest
        - ref-scrambling-script
      automation: hybrid
    - id: test-normalization-intercept
      subject: claim-q14-ole-bridge
      kind: limiting_case
      procedure: "Evaluate the delta=0 expression for P and for O = P / sqrt(2^n), then record which quantity the project will actually plot and how the two conventions translate."
      pass_condition: "The deliverables state either a unit-intercept curve F_0 = 1 or a normalized curve f_0 = 2^-n, plus the exact translation between them."
      evidence_required:
        - deliv-formalism-lock-note
        - deliv-convention-ledger-update
        - ref-algorithmiq-ole
      automation: hybrid
  forbidden_proxies:
    - id: fp-rename-baseline
      subject: claim-q14-ole-bridge
      proxy: "Keep perturbed_echo as the only explicit observable and merely rename it OLE in notes or captions."
      reason: "That would preserve the old state-return observable instead of freezing a fixed-observable operator correlator."
    - id: fp-unfixed-intercept
      subject: claim-q14-ole-bridge
      proxy: "Defer the normalization or intercept choice to Phase 2 and still call Phase 1 complete."
      reason: "Phase 2 cannot produce a trustworthy delta^2 benchmark if the delta=0 target remains ambiguous."
  links:
    - id: link-formalism-note
      source: claim-q14-ole-bridge
      target: deliv-formalism-lock-note
      relation: supports
      verified_by:
        - test-formalism-lock
        - test-normalization-intercept
    - id: link-convention-ledger
      source: claim-q14-ole-bridge
      target: deliv-convention-ledger-update
      relation: supports
      verified_by:
        - test-normalization-intercept
  uncertainty_markers:
    weakest_anchors:
      - "The current project wording mixes a Hilbert-Schmidt-normalized operator condition with a unit-intercept-style small-delta narrative."
    disconfirming_observations:
      - "The chosen observable/generator pair cannot be written in a way that is both explicit and compatible with the q14 manifest and the existing X-kick implementation."
---

<objective>
Freeze the default q14 observable and generator family for the first OLE benchmark, including the normalization and intercept convention.

Purpose: Remove the main semantic blocker before any q14 delta sweep is implemented.
Output: A formalism lock note, convention-ledger updates, and repo-facing wording that separates OLE from perturbed_echo while naming the overlap and disjoint controls.
</objective>

<execution_context>
@/home/bram/.codex/get-physics-done/workflows/execute-plan.md
@/home/bram/.codex/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/01/01-RESEARCH.md
@STATUS.md
@files/quantum-math-lab/benchmarks/q14_only_manifest.json
@files/quantum-math-lab/qiskit_black_hole_scrambling.py
@files/quantum-math-lab/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze the default observable and generator supports</name>
  <files>.gpd/phases/01/ole_formalism_lock.md, files/quantum-math-lab/qiskit_black_hole_scrambling.py, files/quantum-math-lab/benchmarks/q14_only_manifest.json</files>
  <action>Inspect the current q14 manifest and baseline script, then write the formalism lock note using `P = Z_0` as the default fixed observable, `G = X_0` as the overlap generator, and `G = X_10` as the disjoint control unless script inspection exposes a concrete incompatibility. Record the exact qubit labels and why they preserve continuity with the repo's current locality language.</action>
  <verify>Confirm q14 includes qubits `0..13`, the current baseline already uses a local X kick, and the disjoint control does not silently overlap the observable support.</verify>
  <done>The note names one default overlap pair and one disjoint control pair with explicit support labels and no remaining placeholder wording.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve normalization and intercept semantics</name>
  <files>.gpd/phases/01/ole_formalism_lock.md, .gpd/CONVENTIONS.md</files>
  <action>Write the relation between Pauli `P` and normalized `O = P / sqrt(2^n)`, evaluate the `delta = 0` limit for both, and freeze one report-level convention for the plotted q14 curve. If this supersedes the initialization wording, append a new convention entry instead of silently rewriting history.</action>
  <verify>Check `Tr(P^2) = 2^n`, `Tr(O^2) = 1`, and the exact translation between unit-intercept and Hilbert-Schmidt-normalized reporting.</verify>
  <done>The formalism note and conventions ledger state a single unambiguous `delta = 0` target and how to translate any alternative normalization.</done>
</task>

<task type="auto">
  <name>Task 3: Propagate the baseline-versus-OLE guardrail into repo docs</name>
  <files>.gpd/PROJECT.md, files/quantum-math-lab/README.md</files>
  <action>Update project-facing wording so the repo explicitly distinguishes the fixed-observable OLE benchmark from the existing state-return `perturbed_echo` baseline, while naming the Phase 1 fixed observable and generator choices.</action>
  <verify>Check that both documents retain the active q14/q80 scope guardrail, mention explicit `P`/`O` and `G`, and avoid any sentence that labels `perturbed_echo` itself as OLE.</verify>
  <done>The docs explain the bridge without leaving the observable definition or baseline distinction implicit.</done>
</task>

</tasks>

<verification>
- Verify that the chosen report-level OLE quantity is dimensionless and has a stated `delta = 0` value.
- Verify that the overlap and disjoint supports are distinct in the frozen notation and remain compatible with current q14 script assumptions.
- Verify that the repo-facing wording keeps `perturbed_echo` as a baseline only.
</verification>

<success_criteria>
- One default q14 observable/generator pair plus one disjoint control pair are frozen with explicit qubit indices.
- The normalization/intercept ambiguity is resolved in writing and reflected in the conventions ledger.
- Project docs state that `perturbed_echo` remains only the baseline for the new fixed-observable operator correlator.
</success_criteria>

<output>
After completion, create `.gpd/phases/01/01-01-SUMMARY.md`.
</output>
