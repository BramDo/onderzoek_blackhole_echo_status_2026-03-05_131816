---
phase: "01"
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - .gpd/phases/01/ole_small_delta_bridge.md
  - .gpd/phases/01/q14_phase2_handoff.md
  - .gpd/NOTATION_GLOSSARY.md
  - .gpd/ROADMAP.md
interactive: false

conventions:
  units: "dimensionless circuit observables; runtime metrics in seconds"
  metric: "euclidean bookkeeping default"
  coordinates: "Qiskit qubit indices 0..n-1"
  operator_picture: "Carry the derivation in A = U P U^dagger form and translate any U^dagger O U notation explicitly"
  observable_normalization: "Use the Phase 1 lock from 01-01 and state the translation to any alternate normalization"

dimensional_check:
  reported_ole_curve_and_small_delta_coefficient: "[dimensionless]"

approximations:
  - name: "small-delta expansion"
    parameter: "delta << 1"
    validity: "fit window where the intercept and quadratic coefficient remain stable under shrinking delta^2 ranges"
    breaks_when: "higher-order delta terms or hardware noise dominate the onset"
    check: "Require a written nested-fit-window check and intercept-stability criterion in the handoff note"
  - name: "depth-0 locality control"
    parameter: "U = I or effectively zero scrambling depth"
    validity: "support-overlap sanity checks before Heisenberg spreading mixes distant sites"
    breaks_when: "depth is large enough that disjoint controls are no longer approximately local in the evolved operator picture"
    check: "Record depth-0 overlap versus disjoint limits explicitly in the bridge note"

contract:
  schema_version: 1
  scope:
    question: "Given the fixed observable and generator lock from 01-01, what exact small-delta bridge and Phase 2 benchmark interface must the project carry forward?"
  claims:
    - id: claim-q14-ole-bridge
      statement: "With the Phase 1 formalism lock in place, the q14 OLE benchmark can be written as an explicit small-delta quadratic bridge with known delta=0 and depth-0 overlap/disjoint limits that stay distinct from the perturbed_echo baseline."
      deliverables:
        - deliv-small-delta-bridge-note
        - deliv-phase2-handoff-note
      acceptance_tests:
        - test-small-delta-bridge
        - test-phase2-handoff
      references:
        - ref-algorithmiq-ole
        - ref-formalism-lock
        - ref-q14-manifest
        - ref-q14-baseline-artifact
        - ref-status
        - ref-levelb-evidence
        - ref-hardware-protocol
  deliverables:
    - id: deliv-small-delta-bridge-note
      kind: derivation
      path: .gpd/phases/01/ole_small_delta_bridge.md
      description: "Phase note that derives the small-delta q14 OLE bridge in the chosen picture and records the overlap/disjoint limiting cases."
      must_contain:
        - "Defining expression in one fixed operator picture"
        - "Explicit translation to any alternate picture used in the literature"
        - "delta=0 intercept and O(delta^2) coefficient structure"
        - "Depth-0 overlap and disjoint control limits"
    - id: deliv-phase2-handoff-note
      kind: note
      path: .gpd/phases/01/q14_phase2_handoff.md
      description: "Execution-facing handoff note for the q14 exact benchmark and its hardware-ready carry-forward path."
      must_contain:
        - "q14 manifest anchor and current exact baseline artifact"
        - "Required overlap-support and disjoint-support variants"
        - "delta^2 fit-window and intercept-stability checks"
        - "Hardware-ready carry-forward paragraph and q80 subset/full guardrail"
  references:
    - id: ref-algorithmiq-ole
      kind: spec
      locator: "Algorithmiq OLE theory note"
      aliases:
        - "OLE theory PDF"
      role: definition
      why_it_matters: "Provides the target small-delta form and the operator-correlator framing."
      applies_to:
        - claim-q14-ole-bridge
      carry_forward_to:
        - execution
        - verification
        - writing
      must_surface: true
      required_actions:
        - read
        - compare
        - cite
    - id: ref-formalism-lock
      kind: prior_artifact
      locator: ".gpd/phases/01/ole_formalism_lock.md"
      role: must_consider
      why_it_matters: "Provides the frozen observable, generator, support, and normalization choices from 01-01."
      applies_to:
        - claim-q14-ole-bridge
      carry_forward_to:
        - execution
        - verification
      must_surface: true
      required_actions:
        - read
        - use
    - id: ref-q14-manifest
      kind: prior_artifact
      locator: "files/quantum-math-lab/benchmarks/q14_only_manifest.json"
      role: benchmark
      why_it_matters: "Keeps the Phase 2 benchmark tied to the active exact-short task family."
      applies_to:
        - claim-q14-ole-bridge
      carry_forward_to:
        - execution
        - verification
      must_surface: true
      required_actions:
        - read
        - use
        - compare
    - id: ref-q14-baseline-artifact
      kind: prior_artifact
      locator: "files/quantum-math-lab/results/benchmark/classical/black_hole_scrambling_q14_exact_short.json"
      role: benchmark
      why_it_matters: "Supplies the exact-short classical baseline that the first OLE artifact must overlay rather than replace."
      applies_to:
        - claim-q14-ole-bridge
      carry_forward_to:
        - execution
        - verification
      must_surface: true
      required_actions:
        - read
        - use
        - compare
    - id: ref-status
      kind: prior_artifact
      locator: "STATUS.md"
      role: must_consider
      why_it_matters: "Keeps the q14-only claim wording and scope discipline explicit."
      applies_to:
        - claim-q14-ole-bridge
      carry_forward_to:
        - execution
        - verification
        - writing
      must_surface: true
      required_actions:
        - read
        - use
        - avoid
    - id: ref-levelb-evidence
      kind: prior_artifact
      locator: "files/quantum-math-lab/results/hardware/summary/levelb_evidence_report.md"
      role: must_consider
      why_it_matters: "Preserves the q80 subset/full distinction while the hardware-ready story is carried forward."
      applies_to:
        - claim-q14-ole-bridge
      carry_forward_to:
        - execution
        - verification
        - writing
      must_surface: true
      required_actions:
        - read
        - use
        - avoid
    - id: ref-hardware-protocol
      kind: prior_artifact
      locator: "files/quantum-math-lab/hardware_advantage_protocol.md"
      role: method
      why_it_matters: "Constrains how the hardware-ready path can be described without overstating the present evidence."
      applies_to:
        - claim-q14-ole-bridge
      carry_forward_to:
        - execution
        - verification
      must_surface: true
      required_actions:
        - read
        - compare
        - use
  acceptance_tests:
    - id: test-small-delta-bridge
      subject: claim-q14-ole-bridge
      kind: limiting_case
      procedure: "Derive the delta=0 and O(delta^2) terms in the chosen picture, then document the depth-0 overlap and disjoint controls for the frozen Pauli choices."
      pass_condition: "The bridge note states the exact intercept, the quadratic coefficient structure, and the depth-0 limits: overlap is nontrivial while the disjoint control coefficient vanishes."
      evidence_required:
        - deliv-small-delta-bridge-note
        - ref-algorithmiq-ole
        - ref-formalism-lock
      automation: hybrid
    - id: test-phase2-handoff
      subject: deliv-phase2-handoff-note
      kind: benchmark
      procedure: "Check that the handoff note points to one manifest, one existing q14 baseline artifact, the required overlap/disjoint variants, the fit-window tests, and a hardware-ready carry-forward paragraph that keeps q80 subset/full scope explicit."
      pass_condition: "Phase 2 can execute the q14 benchmark without reopening Phase 1 semantics or silently collapsing the hardware story back to a proxy baseline."
      evidence_required:
        - deliv-phase2-handoff-note
        - ref-q14-manifest
        - ref-q14-baseline-artifact
        - ref-status
        - ref-levelb-evidence
        - ref-hardware-protocol
      automation: human
  forbidden_proxies:
    - id: fp-underdetermined-bridge
      subject: claim-q14-ole-bridge
      proxy: "Write a delta-decay story without the explicit picture translation, intercept convention, and overlap/disjoint limiting cases."
      reason: "That would leave Phase 2 free to fit a proxy curve without establishing that the curve is the promised OLE quantity."
    - id: fp-equate-baseline-and-ole
      subject: claim-q14-ole-bridge
      proxy: "Describe the perturbed_echo overlay as an equivalence rather than as a baseline comparison."
      reason: "The project contract requires a bridge from the baseline to OLE, not an identity claim."
  links:
    - id: link-bridge-note
      source: claim-q14-ole-bridge
      target: deliv-small-delta-bridge-note
      relation: supports
      verified_by:
        - test-small-delta-bridge
    - id: link-phase2-handoff
      source: claim-q14-ole-bridge
      target: deliv-phase2-handoff-note
      relation: depends_on
      verified_by:
        - test-phase2-handoff
  uncertainty_markers:
    weakest_anchors:
      - "Phase 1 can only sketch the hardware-ready measurement continuity; the full protocol choice still belongs to Phase 3."
    disconfirming_observations:
      - "The same formalism cannot support both the q14 exact benchmark handoff and the hardware-ready carry-forward note without redefining the observable midstream."
---

<objective>
Write the exact small-delta bridge and the execution-facing handoff for Phase 2, using the frozen Phase 1 observable/generator choices.

Purpose: Turn the formalism lock into a benchmark contract that Phase 2 can execute directly.
Output: A bridge derivation note, a Phase 2 handoff note, and glossary/roadmap wording aligned to the frozen benchmark quantity.
</objective>

<execution_context>
@/home/bram/.codex/get-physics-done/workflows/execute-plan.md
@/home/bram/.codex/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/01/01-RESEARCH.md
@.gpd/phases/01/ole_formalism_lock.md
@STATUS.md
@files/quantum-math-lab/benchmarks/q14_only_manifest.json
@files/quantum-math-lab/results/benchmark/classical/black_hole_scrambling_q14_exact_short.json
@files/quantum-math-lab/hardware_advantage_protocol.md
@files/quantum-math-lab/results/hardware/summary/levelb_evidence_report.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive the Phase 2 bridge in one fixed operator picture</name>
  <files>.gpd/phases/01/ole_small_delta_bridge.md, .gpd/NOTATION_GLOSSARY.md</files>
  <action>Starting from the frozen observable and generator definitions from 01-01, write the small-delta bridge in the `A = U P U^dagger` picture, translate any `U^dagger O U` form explicitly, and record the report-level curve that Phase 2 should plot against `delta^2`.</action>
  <verify>Check the `delta = 0` intercept, the `O(delta^2)` coefficient structure, and the depth-0 control limits for overlap `G = X_0` and disjoint `G = X_10` against `P = Z_0`.</verify>
  <done>The bridge note contains the defining expression, the translated commutator form, and explicit overlap/disjoint limiting cases with no notation drift.</done>
</task>

<task type="auto">
  <name>Task 2: Write the Phase 2 benchmark handoff note</name>
  <files>.gpd/phases/01/q14_phase2_handoff.md, .gpd/ROADMAP.md</files>
  <action>Write an execution-facing handoff note that points Phase 2 to the q14 manifest, the existing classical baseline artifact, the required overlap/disjoint variants, and the exact fit-window and intercept checks needed for the first benchmark figure. Update the roadmap plan wording if the new terminology sharpens the Phase 2 objective.</action>
  <verify>Confirm that the handoff note names one manifest, one baseline artifact, one report-level OLE quantity, and explicit acceptance checks for fit-window stability and baseline overlay.</verify>
  <done>Phase 2 can be executed from the handoff note without reopening the observable choice, picture choice, or normalization choice from Phase 1.</done>
</task>

<task type="auto">
  <name>Task 3: Carry the hardware-ready and q80 scope guardrails forward</name>
  <files>.gpd/phases/01/q14_phase2_handoff.md, .gpd/ROADMAP.md</files>
  <action>Add a short carry-forward section that explains how the q14 exact benchmark remains compatible with the existing hardware runner and why q80 must stay subset-observable in later phases unless a separate full-q80 proxy is justified.</action>
  <verify>Check that the note references the current hardware protocol and Level-B evidence trail, keeps subset/full-q80 language explicit, and does not describe the hardware path as already solved.</verify>
  <done>The handoff note keeps hardware readiness visible and keeps q80 subset/full scope honest without claiming more than the current evidence supports.</done>
</task>

</tasks>

<verification>
- Verify that the chosen benchmark quantity is dimensionless and has a stated `delta = 0` intercept.
- Verify that the depth-0 overlap/disjoint controls are documented as explicit limiting cases, not as vague expectations.
- Verify that the Phase 2 handoff note surfaces the q14 manifest, the current exact baseline artifact, and the hardware/q80 guardrails in one place.
</verification>

<success_criteria>
- The small-`delta` bridge is written in one fixed picture with an explicit translation to any alternative notation used in the literature.
- The Phase 2 handoff note is concrete enough to execute the q14 benchmark without revisiting Phase 1 semantics.
- Hardware-ready continuity and the q80 subset/full split remain visible in the handoff note rather than being deferred into silence.
</success_criteria>

<output>
After completion, create `.gpd/phases/01/01-02-SUMMARY.md`.
</output>
